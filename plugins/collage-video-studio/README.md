# Collage Video Studio Codex plugin

This directory is the installable Codex plugin projection of the repository skill. Source
changes are made at the repository root and synchronized with:

```bash
python scripts/sync_plugin.py --check
```

The plugin contains the complete skill runtime and three screenshots from the deterministic
responsive-direction proof. It requires no account authentication; paid media providers remain
optional and use their own environment variables.
