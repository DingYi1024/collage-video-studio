# Changelog

所有值得记录的变更都会写在这里。版本号遵循语义化版本。

## [Unreleased]

## [6.0.0] - 2026-07-28

### Added

- 新增作品集级 creative-quality 门禁：素材来源、镜头/场景/景别/构图多样性、
  三画幅 director plan、显著主体重复度与 rear/subject/front 纵深覆盖。
- 新增项目自有 `ProductionFilm` Remotion 正式渲染器、全片字幕/音频时间线、
  叙事意图转场和旁白期间自动配乐闪避。
- 新增同运行时 action proof、逐镜头 composition proof、Codex 主机生成素材预约/
  完成/拒绝入口，以及嵌套图层文件内容哈希。
- 新增可编辑 `layout` 节点和三画幅独立覆盖布局。

### Changed

- 实测旁白定帧现在同步缩放所有注册图层包的关键帧、证明时刻和设计停顿。
- 连续旁白母带压缩 TTS 句内异常长静音，同时保留导演控制的语义短停顿。
- 作品集模式拒绝 legacy 拼接器、占位素材、单镜头证明、过量重复和低运动成片。
- 马斯克案例升级为 8 镜原创多图层 Remotion 成片并替换旧案例。

## [5.0.0] - 2026-07-27

### Added

- 新增一等 `looping-environment`：远/中/地面/近景无缝条带、景深速度顺序、
  screen/world 多主体锚定、近景遮挡、镜头补偿位移、带符号轨迹和终点顺序证明。
- 新增三画幅实际世界样片与接缝/覆盖证据；16:9、9:16、1:1 均由同一故事的
  独立导演方案真实渲染 180 帧，而不是横屏裁切。
- 新增 identity-bound 状态素材族：逐状态 facing、identity/部件锚点、锚点覆盖图、
  漂移阈值、共享画布和禁止独立成员替换。
- 新增完整源上下文蒙版修复证明，要求蒙版外及已接受区域逐像素不变。
- 新增 `motion_policy=locked-static`，保护有意静止镜头不被制作档位动画下限污染。
- 新增 readiness seal，将项目、节奏分镜、素材、纯人声、实测时序、字幕、样式/
  构图证明和运行时分面绑定为一个可失效的渲染前封印。
- 新增编码字幕证明、字幕前母版、质量 contact-sheet 生命周期、局部场景预览和
  provider/local/avoided/elapsed 生产指标。
- 新增版本化三风格卡、画幅和视差偏好 intake 决策，以及 Remotion 编辑器中的
  世界样片切换。

### Changed

- 新项目默认要求 intake decision 与 readiness seal；`project_ops.py next` 会返回
  精确缺失步骤，最终渲染拒绝过期 seal。
- composition proof 自动纳入持续世界证据；runtime fingerprint 新增世界、
  字幕交付、质量生命周期、指标和场景预览输入。
- `render.py` 在生成带字幕视觉母版的同时保存无字幕母版，音频改动仍可直接重混。

## [4.0.0] - 2026-07-27

### Added

- 新增强制三方案制作决策：相同语义故事、不同制作深度、执行下限、预期调用、
  本地派生、避免调用、恢复预留、档位上限与人工批准精确上限。
- 新增节奏分镜编译器：每场至少三段连续节奏，建立/动作或峰值/结论证明时刻，
  可见变化处理、语义动作覆盖、素材源包和视听同源事件。
- 新增完整素材源包与注册素材族派生：rear/subject/front 共同画布、三画幅揭示
  范围、主体独立移动范围、完整上下文恢复顺序和稳定家族指纹。
- 新增 provider-native 色键面观测、源 SHA/策略/统计/观测指纹绑定，以及低透明度
  矩形边缘残留检测。
- 新增 recovery-source 生命周期、人工批准上限强制执行、失败/拒绝仍消耗额度的
  精确账本语义，以及并行预留互斥锁，避免多个执行器同时超卖额度。
- 新增 composition、subtitle、audio、provider、protocol 五类运行时分面指纹和
  增量证明失效路由。
- 新增源音频/时序指纹绑定的响度校准，以及导演修订保护和有范围授权的语义修订。
- 新增可复用视觉母版；仅声音或增益改变时使用 `-c:v copy` 直接重混音频。

### Changed

- `studio.py` 与 `project_ops.py next` 现在把三方案、预算审批和节奏分镜作为故事
  审批前的强制可恢复阶段。
- Remotion 工作区新增状态序列、持续显隐、瞬时强调、循环条带、种子母题、
  视听事件、证明时刻跳转和边界证据统计。
- 注册纵深栈现在强制完整角色顺序、共同画布、未裁切成员、严格景深顺序、
  三画幅揭示范围和可见镜头耦合。
- composition proof 会绑定真实构图运行时分面；样式、构图和时刻证明会重新核对
  当前输入，而不是只检查旧报告文件是否存在。

## [3.0.0] - 2026-07-27

### Added

- 新增真实 Remotion/React/Vite 可视化工作区：时间线预览、图层选择、属性检查器、
  JSON 打开/保存、统一 edit point 跳转、三画幅导演切换和 Remotion CLI MP4 渲染。
- 新增数据驱动 SVG 路径/曲线/圆形/文字执行器，以及跨多个标注和安全区的确定性
  碰撞避让；无可用位置时阻断交付。
- 新增身份参考保持、拓扑关系、机制顺序、信息图数值与精确文字的自动语义 QA。
- 新增注册素材纵深栈编译器，将统一画布来源、内容哈希、深度与相机响应绑定。
- 新增 style、composition、moment 三类 proof 和指纹失效检查；案例包含风格证据、
  构图证据与按主张抽取的 proof frames。
- 新增实测旁白时间编译器；配音完成后自动按 30 FPS 重写 beat/shot 整数帧，
  不再裁语音或向旧固定镜头填充长静音。

### Changed

- 语义转场从说明字段升级为实际 FFmpeg xfade 执行路由。
- 色键清理会清零透明像素 RGB，并分别检查透明区残色和半透明边缘去溢色残留。
- `.skill` 和 Codex Plugin 均包含可复现的工作区源码与锁文件，但排除
  `node_modules`、构建缓存和渲染输出。
- 三画幅编辑案例升级为 11 个独立活动层，并加入真实 data-SVG 和标注避让证据。

## [2.0.0] - 2026-07-27

### Added

- 新增递归编辑构图协议：父子组、可编辑文字/形状/图表/时间线/路线/标注、镜头耦合
  景深、显式编辑点，以及同一构图的 16:9、9:16、1:1 三画幅导演方案。
- 新增身份、拓扑、机制、信息图四类语义契约，转场意图路由、旁白实测优先的时长
  编译器和按事实主张精确抽帧的 proof review。
- 新增实际边框取样的色键清理、透明边缘审计，以及素材与构图两道相互独立的质量门。
- 新增追加式供应商事件账本，覆盖预留、完成、失败、拒绝、恢复、替换和复用。
- 新增完整 Codex Plugin、仓库级 Marketplace、同步/失效检查和三画幅插件截图。
- 新增可离线重建的三画幅编辑协议案例，三份成片均为 10 层、10 个活动层。

### Changed

- `state.json` 升级为版本 2，并保留旧 `attempts` 兼容视图；`provider_events` 成为外部
  生成生命周期的审计依据。
- 文案、声音和视觉时长不再依赖“每句固定镜头长度”；优先使用实测人声，缺失时才
  使用语言与节奏感知估算。
- `.skill` 与 Codex Plugin 分别打包，避免插件投影重复进入传统 Skill 压缩包。

## [1.15.0] - 2026-07-27

### Added

- 新增草稿、均衡、全景深三档制作合同，以及写入 `state.json` 的精确生成尝试账本。
- 新增任务、素材和 QA 输入内容指纹；提示词、参数、输入素材或成片变化会自动使旧
  产物或 QA 证据过期。
- 新增注册画布素材构建器、`pose_sequence` 姿态序列、持续 `visibility`、
  `looping_strip` 环境条带和固定种子 `motif_field`。
- 新增 calm/editorial/kinetic 三档成片动态密度检测，独立于重复帧与停帧检查。
- 离线自测覆盖新增图层原语、注册来源、精确预算和指纹失效。

### Changed

- 新建主题/照片项目默认使用均衡档确定性分层管线，以连续关键帧为人物和物体运动
  的首选方案。
- 马斯克案例升级为 62 个透明图层、38 个活动层、注册姿态、持续显隐、循环环境和
  种子母题；旁白改为单一连续资产并附逐句时间证据。
- 案例 QA 现在同时通过 30 FPS 连续性、0 未声明停帧、kinetic 动态密度、纯人声语义
  气口、最终响度和内容指纹门禁。

## [1.14.0] - 2026-07-27

### Added

- 新增全管线成片冻结检测、运动素材时长覆盖检查和镜头级 `designed_holds` 合同。
- 新增纯人声与最终混音的 LUFS/真峰值门禁。
- 后端支持返回含 `timing_path` 的结构化结果，第三方 TTS 时间清单可被安全注册；
  旧 Path 返回值继续兼容并明确标记为缺少时间信息。

### Changed

- 所有新生产项目默认 30 FPS；低于目标帧率的素材默认使用运动补偿插帧，不再复制
  帧制造卡顿。
- QA 同时核对平均/标称帧率、低帧率素材策略、片尾冻结、旁白时间信息和最终音频
  电平。
- 测试后端改为生成真实连续移动的画面和标准语音时间清单，避免静态占位片误导
  管线验收。

## [1.13.3] - 2026-07-26

### Changed

- 归档文件按内部 POSIX 路径排序，不再依赖 Windows/Linux 的 `Path` 大小写排序规则；
  确定性包现在跨平台保持相同文件顺序。

## [1.13.2] - 2026-07-26

### Changed

- 确定性打包进一步统一文本换行为 LF，并改用 `ZIP_STORED`，消除 Windows/Linux
  换行差异和 zlib 版本差异，保证跨平台逐字节一致。

## [1.13.1] - 2026-07-26

### Changed

- `.skill` 打包改为固定时间戳、固定文件模式和稳定排序的确定性 ZIP；同一提交在
  Windows 与 GitHub Actions 上生成相同 SHA-256，方便供应链核验和缓存。

## [1.13.0] - 2026-07-26

### Added

- 新增供应商无关的多语言旁白规划器，支持中、英、日、韩、法、德、西、葡、意大利语
  的 Edge 自动选声。
- 新增 energetic、conversational、measured、dramatic 四种节奏档案及前置文案密度检查。
- 新增逐短语 `.timing.json`，供语义气口 QA 和短句同步字幕共用。

### Changed

- 分句器现在识别英文缩写、首字母缩略词、小数、省略号和中日韩标点。
- 无标点长句会均衡拆成安全短语，避免尾部残留单字或再次形成超长连续朗读。
- `voice_director.py` 默认写入 `media/audio`、自动注册人声及时间清单。
- QA 不再只统计“有几个停顿”，还核对停顿是否出现在计划的语义边界。
- 渲染器优先生成逐短语字幕；旧项目没有时间清单时仍回退为整段字幕。

## [1.12.0] - 2026-07-26

### Added

- 旁白生成新增逗号、分句、整句和段落四级气口；去除供应商首尾填充后，默认分别
  追加 0.10、0.16、0.22 和 0.26 秒。
- 纯人声 QA 新增语义边界气口覆盖率与最长连续发声检测。

### Changed

- “连续旁白”重新定义为同一音色、同一时间轴上的有节奏表演，不再等同于一次性
  合成长句。
- 默认要求至少 75% 的语义边界具有不少于 0.16 秒的完整气口，且最长连续朗读不
  超过 5.5 秒。
- `voice_director.py` 按标点拆分短语、去除供应商首尾填充，再以确定性气口重新拼接。

## [1.11.0] - 2026-07-26

### Added

- 新增纯人声音轨 `audio_qa.py`，检测句内、片段间、开头和结尾静音，以及总静音占比。
- 新增 `voice-continuity.md`，规定自然句间停顿、连续旁白和交付阻断标准。

### Changed

- 新项目默认使用 `audio.voice.continuity_mode: "continuous"`，整片生成并注册
  `voice:main`；缺少该字段的旧项目继续兼容分段旁白。
- `voice_director.py` 在补齐场景时长前先检查真实语音时长和静音，不再允许用 `apad`
  掩盖过短文案。
- 最终 QA 在混入音乐前检查纯人声；音轨存在和总时长正确不再代表音频合格。

## [1.10.0] - 2026-07-26

### Added

- 新增 `smooth-keyframes.md` 丝滑关键帧制作协议，并加入连续运动中间关键帧停顿审计。
- 图层可声明 `motion_intent: "continuous"`；审计可通过
  `enforce_smooth_keyframes` 拒绝重复缓动造成的停一下、再启动。

### Changed

- 默认人物运动从全身关节和真实步态改为稳定整身剪纸或少量分组图层的连续关键帧
  位移；关节与双腿步态保留为近景、故事关键动作的可选能力。
- 图层任务提示改为优先 Catmull–Rom 连续速度、交错时间和交付帧率审计。

## [1.9.0] - 2026-07-26

### Added

- 新增全身 `locomotion` rig 契约，要求两只脚、两条关节链、有效根节点位移和交替
  落脚区间。
- 新增左右脚 x/y 成对锁定、最小落脚时长、最大双脚支撑重叠和最大落脚漂移校验。
- 运动审计和 QA 新增 `locomotion_rigs` 与 `plant_intervals` 统计。
- 新增 9:16 全身纸偶步态基准，使用双关节逆运动学生成 13 层、10 个活动层、
  8 个层级关节和 4 个落脚区间。
- 新增 `locomotion.md` 步态制作与验收协议。

### Changed

- 图层任务提示会要求移动角色声明双腿关节链和交替双轴落脚锁，不再允许整张人物
  贴纸横向滑动。
- 创意 QA 审批改用最终视频 SHA-256 内容签名，同时继续兼容旧版时间戳签名。

### Fixed

- 修复 Skill 从 GitHub 安装、复制或解压后，仅因 `final.mp4` 文件时间变化就把
  `creative-qa` 错误标记为过期的问题。

## [1.8.0] - 2026-07-25

### Added

- 新增 `follow.space: "rig"` 层级关节空间，子层支点会跟随父层解析后的位移、旋转
  和缩放，不再只是世界坐标平移。
- 新增 `articulated-paper` rig 连通性校验，拒绝缺失根节点、断链部件、关节延迟和
  非完整 x/y 继承。
- 新增 `articulated-rigs.md`，定义全画布刚性部件、肩肘支点、落脚锁定和五连帧复核。
- 逐帧运动审计新增 `rig_followers` 统计。

### Changed

- 马斯克案例第四镜加入躯干—上臂—前臂三段纸偶，人物伸臂推动资本分流，并以脚点
  双轴锁定替代整个人物滑动。
- 案例增至 57 个透明图层、33 个活动层和 5 个父子跟随层，其中 2 个为层级关节。
- 案例新增人物关节五连帧证据图和对应创意复核条目。
- 图层任务提示会明确要求 connected limb 使用 rig-space，禁止大姿态交叉淡化和
  无步态角色滑行。

### Fixed

- 修复父层旋转或缩放时子关节仍停在世界坐标、产生肩肘脱节的问题。

## [1.7.0] - 2026-07-25

### Added

- 新增父子图层 `follow` 关系，可按权重继承位移、旋转、缩放和透明度，并支持短延迟。
- 新增逐帧运动连续性审计，检测速度、角速度、缩放和透明度的一帧突变。
- 新增 `direction.contacts`，自动检测纸片落地、车轮着地或证据卡落定后的滑移。
- 新增 `direction.secondary_responses`，要求每个从属动作说明驱动图层与物理原因。
- 新增 `motion-audit.md` 和 `layer_compositor.py --audit` 命令。

### Changed

- 马斯克案例第五镜拆分汽车轮子和火箭尾焰，三个从属层由主物体驱动，不再独立漂移。
- QA 报告记录每镜运动峰值、跟随层数量和接触锁结果。
- 图层任务提示会主动要求从属关系、落地接触锁和帧级连续性审计。

### Fixed

- 拒绝重复时间关键帧、父子图层循环引用、缺失父层和落定后缓慢滑移。
- 火箭尾焰改为连续渐入渐出，避免透明度与纵向缩放突变。
- 案例纸纹改为种子化确定性噪声，相同输入重建时图层哈希保持一致。

## [1.6.0] - 2026-07-25

### Added

- 新增 `directed-motion.md` 导演协议：一镜一个主动作、物理原因、运动密度，以及
  anticipation/action/settle 三段连续时间。
- `layers.json` 新增定向动作校验、主图层校验和有理由的 `designed_holds`。
- 关键帧新增分段 `ease`，支持 `back-in`、`back-out`、`back-in-out` 和 `hold`。
- 动态 QA 新增运动密度与不可见微动警告，并允许登记后的证据阅读停顿。
- 新增 24 秒“马斯克成为首富的路径”完整案例、事实来源和六镜动作拆解。

### Changed

- 动态质量从“尽量多的图层持续活动”改为“少量主图层完成有原因的动作并回稳”。
- 全帧停顿检测降低噪声容差，避免把小范围纸片运动误判为整帧冻结。
- 案例改用六种景别/取景、51 个透明层、27 个活动层和自然普通话男声。
- 主 README 的默认案例由城市热岛替换为马斯克首富路径。

### Fixed

- 避免重复背景裁切、人物循环闪现、全层微抖和永久环境循环造成的机械玩具感。
- 设计停顿不再与意外停帧使用同一失败规则。

## [1.5.0] - 2026-07-25

### Added

- 新增 `--aspect auto`：按平台、主体数量和空间叙事自动选择横竖画幅并记录原因。
- 新增刚性剪纸运动类别、铰链支点校验和大姿态交叉淡化拦截。
- 新增 `aspect-direction.md` 与蝴蝶三件式纸偶、人物落脚约束规范。

### Changed

- 完整案例改为原生 16:9 横屏纸艺场景，交付 1920×1080、30 FPS 成片。
- 人物改为“一镜一个稳定全身姿态”，姿态变化只发生在镜头切换处。
- 蝴蝶改为身体、左翅、右翅三个刚性图层，共用短距离根运动并在翼根转动。
- 案例增至 48 个独立透明活动层，移除跨屏贴纸滑行和整身轮廓交叉淡化。

### Fixed

- 安装包构建排除 `dist/`，避免把输出临时包递归打进自身。

## [1.3.0] - 2026-07-25

### Added

- 新增 Catmull‑Rom 连续速度曲线、循环相位错峰和六种基础缓动。
- 新增 1–4× 子像素超采样与可选多时间采样运动模糊。
- 新增可配置 FFmpeg 镜头转场，支持擦拭、滑动、溶解、平滑推移和淡化。
- 新增 30 FPS 轻量 MP4 案例预览。

### Changed

- 案例成片从 24 FPS 升级到恒定 30 FPS，共 480 帧。
- GitHub 动态 GIF 从 6 FPS 升级到 15 FPS。
- 重排全部案例关键帧与循环相位，消除图层在 0/2/4 秒同步停顿。
- 人工 QA 现在检查意外停顿和跳帧，同时允许设计明确的无缝循环。

## [1.2.0] - 2026-07-24

### Added

- 新增 `layers` 生产阶段、`layer_package` 和 `layers_to_video` 任务类型。
- 新增通用 `layers.json` 协议和全画布透明 PNG 图层规范。
- 新增确定性多图层合成器，支持位移、缩放、单轴缩放、旋转、透明度和缓动。
- 新增分层动态 QA，逐镜头检查最少图层数与独立活动层数。
- 新增多图层合成自测和完整分层制作参考。

### Changed

- 城市热岛案例从整张关键帧推拉重做为真实多图层动画。
- 四个案例镜头现在包含 30 个透明图层和 30 个独立活动层。
- 案例新增 `layers.jsonl`、图层源文件、可重复图层生成器和分层 QA 证据。

## [1.1.0] - 2026-07-23

### Added

- 新增“同一座城市，为什么会有两个温度？”完整可重建案例。
- 新增同场景三套视觉方向、四张真实关键帧、普通话旁白和原创合成底乐。
- 新增离线案例 adapter，复用正式 JSONL 任务、状态登记、审批、渲染和 QA 接口。
- 新增 16 秒 1080×1920 成品、GIF 预览、八帧效果总览和技术 QA 结果。
- 新增从 Brief 到最终审批的中文逐步实现说明。

### Changed

- `.skill` 安装包排除仓库专用的案例媒体，保持安装体积精简。

## [1.0.1] - 2026-07-23

### Fixed

- 声明 Pillow 运行依赖，并在 GitHub Actions 自测前安装 Python 依赖。

## [1.0.0] - 2026-07-23

### Added

- 主题、照片和已有视频三种制作模式。
- 故事、视觉方向和最终创意 QA 审批门。
- 六类中立媒体任务和可插拔后端协议。
- Replicate 生产适配器及项目级模型映射。
- 预测轮询恢复、防重复提交、不确定提交保护和显式释放流程。
- FFmpeg 渲染、FFprobe QA、检查点、报告和交付归档。
- 离线全流程自测和可安装 Skill 打包。
- GitHub CI、Tag 发布和依赖更新工作流。
- Skills CLI 一键安装说明和 MIT 许可证。
